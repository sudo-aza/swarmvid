# Researcher Journal — 2026-05-28 21:39 UTC+8

## Turn Summary

**Type**: Fallback review pass (no pending Researcher tasks)
**Task**: Paragraph splitting research for Task #209 (ghost narrowing non-penalty fix)
**Outcome**: Research note created — 3 approaches identified, `lua-widow-control` technique is the breakthrough

## What I Did

### 1. BLACKBOARD Check
- Fetched BLACKBOARD.md via GitHub API (git clone timed out — likely VM network issue)
- All Researcher tasks (#1-4, #26, #30, #133) confirmed **done**
- No new Researcher tasks assigned since last run (2026-05-18)
- Triggered fallback pipeline

### 2. Cross-Team Context Review
- **QA**: 21 consecutive stand-down turns, waiting on Programmer Task #209
- **Programmer**: v3.54 released, 7 penalty-based ghost-narrowing attempts exhausted, Task #209 pending (non-penalty approach)
- **zoe**: Chose option (b) from Task #208 — wants fundamentally different approach, released v3.54 tag
- **Key blocker**: Programmer stuck on "how to fix ghost narrowing without penalties"

### 3. Research: Novel Approaches for Task #209
Conducted 30+ web searches focused on paragraph splitting, `tex.linebreak()` from callbacks, and parshape manipulation. Key findings:

**Breakthrough discovery**: `lua-widow-control` package (v3.0.1, CTAN, 2024) proves that `tex.linebreak()` can be called from within LuaTeX callbacks with a modified node list. This is a production-proven technique used for widow/orphan control. It enables a "split-and-re-linebreak" approach that is fundamentally different from the 7 failed penalty attempts.

**Three approaches identified** (documented in notes/2026-05-28-paragraph-splitting-research.md):
1. **Approach A — Split-and-Re-Linebreak** (HIGH feasibility): Use `post_linebreak_filter` to detect ghost narrowing risk, split the node list, and call `tex.linebreak()` separately for narrowed and full-width portions. Based directly on lua-widow-control's technique.
2. **Approach B — Pre-Check Needspace** (HIGH feasibility, simplest): At `\everypar` time, check remaining page space against narrowed zone height. If insufficient, force `\newpage` before the paragraph. 5-10 lines of Lua.
3. **Approach C — Hybrid Linebreak Filter** (MEDIUM feasibility): Register `linebreak_filter` that wraps TeX's built-in breaker with ghost-narrowing prediction. Most elegant but most complex.

### 4. Additional Techniques Cataloged
- `tex.parshape` read/write from Lua
- `pre_linebreak_filter` with parshape modification (chickenize package)
- `linebreak_filter` complete paragraph builder replacement (ConTeXt node-ltp.lua)
- `\needspace` preventive page-break detection
- LaTeX 2020+ paragraph hooks (`\AtBeginParagraph`, etc.)
- Mittelbach's globally optimized pagination framework
- ε-TeX parshape introspection (`\parshapelines`, `\parshapeindent`, `\parshapelength`)

### 5. Task #211 Created
Added Task #211 to BLACKBOARD.md for Programmer — concrete implementation guidance based on this research, with Approach B as the recommended starting point (simplest, quickest win).

## Assessment

This research directly unblocks Task #209. The `lua-widow-control` technique is the missing piece — it proves that `tex.linebreak()` can be called from callbacks, which opens up split-and-re-linebreak as a viable non-penalty approach. The Programmer should study the lua-widow-control source code and prototype Approach B (Pre-Check Needspace) first — it's the quickest win and could eliminate the most common ghost narrowing pattern.

## Issues Encountered

- **git clone timeout**: VM network issue — GitHub API worked fine but git clone consistently timed out. Worked around by using GitHub REST API for all file operations.
- **Cron job status**: Last execution was 2026-05-19 (9 days ago) with "model concurrency limit exceeded" error. The cron is configured correctly (daily 10:00 UTC+8) but may need re-enabling.
