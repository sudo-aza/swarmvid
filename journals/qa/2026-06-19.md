# QA Journal — 2026-06-19

## T170 — Migration Turn

**Time**: 2026-06-19 22:55 +08:00
**Type**: Migration (not a standard QA turn)

### What happened

User (zoe) instructed: "LaTeX is done. Switch to the swarmvid repo. Update all cron prompts etc accordingly. You keep your old role, all of the old rules which fit."

### Actions taken

1. **Deleted old cron** — LaTeX QA cron job 192521 removed
2. **Cloned swarmvid** — `sudo-aza/swarmvid` from GitHub
3. **Created `notes/qa-rules.md`** — 14 rules adapted from LaTeX swarm QA rules
4. **Created new cron** — Job 217313, hourly at :30 Asia/Shanghai
5. **Committed and pushed**

### BLACKBOARD state

- Task #1 (video assembly): done
- Task #2 (upload video): pending, not a QA task
- No QA tasks pending

## T171 — Active Inspection

**Time**: 2026-06-19 23:10 +08:00
**Type**: Active inspection (Rule 6 — no pending QA tasks)

### What was inspected

- `scripts/render_scene.py` (299 lines) — Pillow frame renderer
- `scripts/assemble_video.py` (82 lines) — ffmpeg scene concatenator
- `BLACKBOARD.md` — structure and formatting

### Bugs found

**Bug 1 (CRITICAL): render_scene.py alpha compositing broken**

The script creates an `Image.new("RGBA")` and draws multiple elements with alpha fill values:
- Line 77: particles `fill=(255, 255, 255, alpha)` — intended as subtle transparent dots
- Line 113: header bar `fill=(0, 0, 0, 100)` — intended as semi-transparent overlay
- Lines 144-147: text box `fill=(0, 0, 0, 80)` — intended as semi-transparent backdrop
- Line 161: text fade-in `fill=(240, 240, 240, line_alpha)` — intended as gradual opacity
- Line 176: source text `fill=(150, 150, 150, 180)` — intended as subtle text

But line 178 calls `img.convert("RGB")` which **drops the alpha channel entirely**. Verified with a test: a pixel drawn as `(255, 255, 255, 50)` becomes `(255, 255, 255)` after conversion — fully opaque white.

Result: ALL transparency effects are broken. Particles will be solid white dots, overlays will be solid black, text fade-in won't work. The only correct alpha compositing in the script is the title card (lines 121-132) which properly uses `Image.alpha_composite()`.

Reported as BLACKBOARD Task #6.

**Bug 2 (MEDIUM): assemble_video.py crossfade parameter is a lie**

The function signature accepts `crossfade_s=1.0` and the docstring says "with crossfade transitions." But line 28's comment admits "no crossfade for reliability" and the parameter is never referenced in the function body. The actual implementation uses `ffmpeg -f concat` which produces hard cuts between scenes.

Per the video spec, "smooth scene transitions" are required. Either implement crossfades or remove the misleading parameter/docstring.

Reported as BLACKBOARD Task #7.

**Minor: Duplicate TODO table header**

Lines 83-86 had two identical `| # | Task | Assigned To | Status | Created |` header rows. Fixed.

### BLACKBOARD state after this turn

- Tasks #1-5: pending (Writer/Producer pipeline)
- Task #6: pending, Programmer — alpha compositing fix
- Task #7: pending, Programmer — crossfade fix
- No QA tasks yet

## T172 — Cron Turn (no QA tasks)

**Time**: 2026-06-19 23:30 +08:00
**Type**: Automated cron turn

### What happened

Pulled latest. Programmer reverted narration script (commit bc551c0) — correctly reassigned task #1 to Writer. Deleted 28 scene JSONs and generate_scenes.py from repo (aligns with "no scripts/ committed" production note, though the pipeline scripts themselves remain).

### Active inspection (Rule 6)

- **Repo integrity**: Clean — 6 text files, 2 pipeline scripts (render_scene.py, assemble_video.py)
- **Scripts unchanged**: Both files identical to last review. Bugs #6 (alpha) and #7 (crossfade) still present and open on BLACKBOARD
- **Task #1 reverted**: Now "pending" assigned to Writer (was briefly "done" by Programmer — correct self-correction)
- **No new issues found**

## T173 — Cron Turn (no QA tasks)

**Time**: 2026-06-20 00:30 +08:00
**Type**: Automated cron turn

### What happened

Pulled latest. Programmer fixed Task #6 (alpha compositing) in commit eb1a87a. Two new Programmer commits also present: TTS spec fixes (6dc59c6, 5d0c407) and BLACKBOARD update (ead8f3d).

### Active inspection (Rule 6)

**Task #6 verification — Independent alpha test:**

Ran a 3-pixel verification test simulating the Programmer's overlay approach:
1. Created base RGBA with 2-color gradient (matching render_scene.py pattern)
2. Drew header bar overlay `fill=(0,0,0,100)` on separate RGBA, composited via `Image.alpha_composite()`
3. Drew text box overlay `fill=(0,0,0,80)`, composited same way
4. Checked `convert("RGB")` preserves composited result

Results:
- Header bar pixel (100,30): RGB(15,16,29) — correct alpha blend, NOT solid black ✅
- Text box pixel (500,400): RGB(13,20,50) — correct alpha blend ✅
- After convert("RGB"): RGB(15,16,29) — preserved ✅

**Fix architecture review:**
- Particles: pre-blended against gradient background, drawn at alpha=255 on transparent overlay — valid approach
- Header bar, text box, progress bar, sources: each on separate RGBA overlay composited via `Image.alpha_composite()` — correct
- Title card: overlay approach preserved from original — correct
- Final `convert("RGB")` at line 222 happens after all compositing — no longer drops alpha
- The fix is sound and addresses the root cause completely

**Dead code found (Rule 4.5):**
- `blend_text_color()` function (lines 123-126) is defined but never called
- AST analysis confirmed zero call sites in the entire script
- Likely a helper written during the fix but ultimately unused (text alpha handled by overlay approach instead)
- Reported as BLACKBOARD Task #8

**Other checks:**
- assemble_video.py unchanged — Task #7 (crossfade) still pending
- Repo integrity: clean, all expected files present
- No QA tasks on BLACKBOARD — pipeline hasn't produced reviewable deliverables yet

### BLACKBOARD state after this turn

- Tasks #1-5: pending (Writer/Producer pipeline)
- Task #6: **done** (Programmer fixed alpha compositing, verified by QA)
- Task #7: pending (Programmer) — crossfade still not fixed
- Task #8: pending (Programmer) — dead code removal (NEW, created by QA this turn)

## T174 — Manual Trigger (no QA tasks)

**Time**: 2026-06-20 01:07 +08:00
**Type**: Manual trigger ("do a task") — active inspection

### What happened

Pulled latest. Programmer pushed 2 fixes since T173:
- Task #7: `assemble_video.py` crossfade implemented (commit f294218, +190/-37 lines)
- Task #8: `blend_text_color()` dead code removed from `render_scene.py` (same commit)

### Active inspection (Rule 6)

**Task #7 verification — Crossfade filter chain analysis:**

Ran 4 simulation tests of the filter chain construction logic:
1. **3 scenes x 5s, 1s crossfade**: offsets [4.0, 8.0], 2 v-xfade + 2 a-acrossfade, expected 13s ✅
2. **28 scenes x 180s, 1s crossfade**: 27 v-xfade + 27 a-acrossfade, all 26 intermediate label pairs ([vt0]-[vt25], [at0]-[at25]) present, [vout]/[aout] terminal labels confirmed, expected 5013s (83.5 min) ✅
3. **2 scenes x 5s, 0.5s crossfade**: single filter pair, offset 4.5, expected 9.5s ✅
4. **5 scenes label chain integrity**: [vt0]→[vt1]→[vt2]→[vout], [at0]→[at1]→[at2]→[aout] all properly chained ✅

Code review findings:
- `get_duration()` helper uses ffprobe with JSON output — robust error handling ✅
- Sequential label routing pattern ([0:v][1:v]→[vt0], [vt0][2:v]→[vt1], ...) — correct ffmpeg filter_complex usage ✅
- Offset math: `durations[0] - crossfade_s` for first, then `+= durations[i+1] - crossfade_s` — correct ✅
- `acrossfade` audio filter with `c1=tri:c2=tri` curves — appropriate for documentary narration ✅
- Rotating transitions (fade, slideright, fadeblack, smoothleft, circleopen) — adds visual variety ✅
- `--no-crossfade` fallback preserves old concat behavior ✅
- Single-scene edge case handled (lines 45-60) ✅
- Duration validation: rejects scenes shorter than crossfade duration ✅

**Task #8 verification — Dead code removal:**
- AST analysis confirmed `blend_text_color` is no longer defined in render_scene.py ✅
- Function list: hex_to_rgb, lerp_color, make_gradient, draw_particles, build_bg_pixel_row, wrap_text, render_frame, get_fonts, render_scene, main — all used ✅

**No new issues found.**

### BLACKBOARD state after this turn

- Tasks #1-5: pending (Writer/Producer pipeline)
- Task #6: done (alpha fix, verified T173)
- Task #7: done (crossfade fix, verified this turn)
- Task #8: done (dead code removed, verified this turn)
- All Programmer tasks complete. Waiting on Writer to produce scripts.

## T175 — Cron Turn (no QA tasks)

**Time**: 2026-06-20 01:30 +08:00
**Type**: Automated cron turn

### What happened

Pulled latest. Programmer self-tasked #9: added `has_audio_stream()` and `ensure_audio()` to `assemble_video.py` (commit 13c5a1c, +68/-5 lines).

### Active inspection (Rule 6)

**Task #9 verification — Audio stream handling:**

Code review of new functions:
- `has_audio_stream()` (lines 38-51): ffprobe with `-select_streams a -show_streams -of json`, returns bool. Correct error handling (returns False on failure). ✅
- `ensure_audio()` (lines 54-85): Iterates scenes, checks audio, creates temp copies with `anullsrc=channel_layout=stereo:sample_rate=44100` + `-c:v copy -c:a aac -b:a 192k -shortest` for audio-less scenes. Returns `(file_list, had_temps)` tuple. ✅
- Integration in `assemble()` (lines 271-284): Creates `.assemble_tmp` dir, calls `ensure_audio`, uses `try/finally` for cleanup with `shutil.rmtree`. ✅

Logic tests:
- `had_temps` zip comparison: correctly detects 1 replacement (True) and 0 replacements (False) ✅
- All 7 functions in assemble_video.py are called — no dead code ✅
- render_scene.py unchanged since T174 ✅

Minor style note (not a bug): `import shutil` is inside `assemble()` body (line 263) instead of at module top with other imports.

**No new issues found.**

### BLACKBOARD state after this turn

- Tasks #1-5: pending (Writer/Producer pipeline)
- Tasks #6-9: done (all Programmer fixes verified)
- Waiting on Writer for narration scripts

---

## T176 — Active Inspection (03:30)

**Time**: 2026-06-20 03:30 +08:00
**Type**: Cron trigger (Rule 6 — no pending QA tasks)
**Commits reviewed**: 7ab8fbc (Task #15: pipeline.py TTS duration measurement)

### No QA tasks on BLACKBOARD

Tasks #1-5 pending (Writer/Producer). Tasks #6-9, #10-12, #15 done (Programmer). Tasks #13-14 pending (Writer). No QA-assigned tasks.

### Active inspection (Rule 6)

**Commit 7ab8fbc — Task #15: TTS duration measurement in pipeline.py**

Reviewed the diff (+32/-2 lines). Changes:
- Added `import wave as wave_mod` (line 33)
- Added `get_wav_duration()` (lines 38-53): Uses `wave` module with ffprobe fallback. Correct implementation. ✅
- Added `actual_durations` list and `scene_dirty` flag per scene
- After successful TTS generation: measures actual duration, updates `segment["duration_s"]` if differs > 0.5s
- Writes scene JSON only if `scene_dirty` is True (avoids unnecessary disk writes)
- On TTS failure: creates silent placeholder, keeps default duration ✅
- Added `total_duration` counter and prints total audio time

**Full pipeline.py review (first review of this file):**
- 7 functions defined: `get_wav_duration`, `run_cmd`, `step_parse_narration`, `step_generate_tts`, `step_concat_audio`, `step_render_scenes`, `step_assemble`, `main`
- AST analysis: `run_cmd()` is defined but never called — dead code ⚠️
- `import shutil` (line 31) is unused — dead import ⚠️
- `actual_durations` list is populated but never read — minor dead code (not reported, likely future use)
- All 5 step functions and `main()` are properly called ✅
- Pipeline flow: parse → TTS → concat → render → assemble — correct order ✅
- Resume support: skips existing files > 1000 bytes (audio) or > 10000 bytes (video) ✅
- Rate limiting: `time.sleep(tts_delay)` between TTS calls ✅
- TTS CLI: `z-ai tts -i <text> -o <file> --voice <voice> --format wav` — correct per spec ✅

**parse_narration.py review (commit e206ddf, not previously reviewed by QA):**
- 3 functions: `parse_blackboard`, `build_scene_json`, `main` — all called, no dead code ✅
- Regex for scene headers: correctly handles both `Title — Subtitle` and `Title`-only formats ✅
- Regex for segment markers: `\[S<N>\.\d+\]` — verified no prefix collision (e.g., S1 pattern does not match S10) ✅
- Bounds narration section between `## Narration Script` and `## TODO` ✅
- Era/gradient/accent assignment via cycling — correct ✅
- Segment text: collapses whitespace, strips, verifies < 1024 chars ✅
- Sources: hardcoded to `[]` — known issue (Task #14)

**Communication Log deletion (third occurrence):**
- Task #15 commit (7ab8fbc) deleted the Communication Log section that Task #12 (b5ae9ce) had just restored
- This is a recurring process problem — agents overwrite the entire BLACKBOARD.md without preserving the Communication Log
- Created Task #16 for Programmer to restore it again
- Added stronger wording in task description: "MUST preserve the Communication Log section"

**Repo integrity:**
- 4 scripts in `scripts/`: render_scene.py, assemble_video.py, parse_narration.py, pipeline.py
- No binary files in git ✅
- Clean commit history ✅

### Findings reported to BLACKBOARD (Rule 4.5)

- Task #16: Restore Communication Log (3rd deletion)
- Task #17: Remove dead code from pipeline.py (`run_cmd()` + `import shutil`)

---

## T177 — Active Inspection (04:30)

**Time**: 2026-06-20 04:30 +08:00
**Type**: Cron trigger (Rule 6 — no pending QA tasks)
**Commits reviewed**: 8599ad9 (Task #17: remove dead code from pipeline.py)

### No QA tasks on BLACKBOARD

Tasks #1-5 pending (Writer/Producer). Tasks #6-12, #15-17 done (Programmer). Tasks #13-14 pending (Writer). Task #18 created this turn (Programmer). No QA-assigned tasks.

### Active inspection (Rule 6)

**Commit 8599ad9 — Task #17: Remove dead code from pipeline.py**

Diff review (+3/-14 lines):
- Removed `import shutil` (was unused) ✅
- Removed `run_cmd()` function (12 lines, was never called) ✅
- Left one extra blank line where `run_cmd` was (line 55) — cosmetic, not a bug

AST analysis post-fix:
- 7 functions: `get_wav_duration`, `step_parse_narration`, `step_generate_tts`, `step_concat_audio`, `step_render_scenes`, `step_assemble`, `main`
- All 7 called — zero dead code ✅
- All imports used ✅
- `import shutil` gone ✅

**Binary file in git (NEW FINDING):**
- Commit 8599ad9 added `scripts/__pycache__/pipeline.cpython-312.pyc` (21KB binary)
- This is compiled Python bytecode — should never be in version control
- Root cause: no `.gitignore` file exists in the repo
- Programmer likely ran pipeline.py locally, Python created `__pycache__/`, and used `git add -A`
- Reported as Task #18

**Communication Log:** Preserved correctly by Programmer in this commit ✅

### Findings reported to BLACKBOARD (Rule 4.5)

- Task #18: Remove `__pycache__` from git and add `.gitignore`

---

## T178 — Active Inspection (05:30)

**Time**: 2026-06-20 05:30 +08:00
**Type**: Cron trigger (Rule 6 — no pending QA tasks)
**Commits reviewed**: 48237b3 (Task #18: remove __pycache__ from git, add .gitignore)

### No QA tasks on BLACKBOARD

Tasks #2-5 pending (Writer/Producer). Tasks #13-14 pending (Writer). All Programmer tasks (#6-12, #15-18) done. No QA-assigned tasks.

### Active inspection (Rule 6)

**Commit 48237b3 — Task #18 verification:**

- `scripts/__pycache__/pipeline.cpython-312.pyc` removed from git tracking ✅
- `.gitignore` created with 19 lines covering:
  - Python: `__pycache__/`, `*.pyc`, `*.pyo` ✅
  - Pipeline outputs: `output/`, `*.mp4`, `*.wav` ✅
  - OS/editor: `.DS_Store`, `Thumbs.db`, `*.swp`, `*~` ✅
  - IDE: `.idea/`, `.vscode/` ✅
- Verified: `git ls-files` returns no `.pyc`, `.pyo`, `.mp4`, `.wav`, `.png`, or `__pycache__` entries ✅
- Communication Log preserved and appended to correctly ✅
- BLACKBOARD diff: Task #18 marked done, Comm Log entry added, no other changes ✅

**Repo integrity:**
- 4 scripts in `scripts/`: render_scene.py, assemble_video.py, parse_narration.py, pipeline.py
- `.gitignore` present and effective
- No binaries tracked ✅
- Clean commit history (22 commits) ✅

**No new issues found this turn.**