# QA Journal — 2026-06-20

## T176 — Cron Turn (no QA tasks)

**Time**: 2026-06-20 02:30 +08:00
**Type**: Automated cron turn

### What happened

Pulled latest. Repo was re-cloned (session reset). Major changes since T175:
- Writer completed Task #1: full 28-scene German narration (commit 45a070a, +736 lines)
- Programmer added parse_narration.py (commit e206ddf, 226 lines) and pipeline.py (commit 8cd2201, 374 lines)

### Active inspection (Rule 6)

**Narration script review:**
- 28 scenes, 218 segments — matches expected count
- All segments under 1024 chars (max 951, S5.7) — within TTS API limit
- Minimum segment length 134 chars (S13.5) — no dangerously short segments
- Quality: Excellent German documentary style (ZDF/ARD level). Specific dates, names, events throughout. Scene 1 covers prehistoric settlements through Charlemagne with proper historical context.
- Scene headers use `### Szene N: Title — Subtitle` format — correctly parsed by parse_narration.py
- Scene 28 subtitle "Eine Stadt im Wandel" correctly captured

**parse_narration.py review:**
- Tested: successfully parsed 28 scenes / 218 segments
- All scene JSONs have correct structure (scene_num, title, subtitle, era, segments, sources, gradient, accent)
- All segment texts under 1024 chars
- Gradient rotation (8 palettes) and accent rotation (8 colors) provide visual variety
- Era assignment matches original spec for all 28 scenes

**pipeline.py review:**
- 5-step orchestration: parse → TTS → concat audio → render → assemble
- Uses `z-ai tts` CLI for TTS generation
- Resume support via `--start-from` and file-existence checks
- Silent placeholder on TTS failure (resilient but means final video could have gaps)
- `--tts-only` and `--render-only` flags for partial runs
- Import of parse_narration.py uses bare `from parse_narration import ...` (works because Python adds script dir to sys.path)

**Issues found (Rule 4.5):**

1. **Communication Log section DELETED** (CRITICAL): Writer's commit (45a070a) removed the Communication Log entirely. This section is required by QA Rule 4 (PASS/FAIL workflow), Programmer rules, and all agent coordination. Restored it with condensed history. → Task #12

2. **Scene Breakdown table DELETED**: The original table had target durations per scene (180s, 240s, 300s). QA Rule 10 requires duration verification: "Each scene should match the scene breakdown durations (+/- 10s tolerance)." Without this reference, QA cannot perform duration checks. → Task #13

3. **Empty sources in all scene JSONs**: All 28 scenes have `"sources": []`. The spec (Production Notes, qa-rules.md Section 10) requires "Sources must appear on-screen during relevant scenes." The narration text mentions historical events but contains no citations. → Task #14

### BLACKBOARD state after this turn

- Tasks #1, #6-#11: done
- Tasks #2-#5: pending (Producer)
- Task #12: pending (Programmer) — restore Communication Log
- Task #13: pending (Writer) — restore target durations
- Task #14: pending (Writer) — add source citations
- Communication Log restored by QA with condensed history