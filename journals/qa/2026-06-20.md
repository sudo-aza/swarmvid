# QA Journal — 2026-06-20

## T179 — Active Inspection (06:30)

**Time**: 2026-06-20 06:30 +08:00
**Type**: Cron trigger (Rule 6 — no pending QA tasks)
**Commits reviewed**: 3554233, 2199100, c0d95fd, 676b437 (Programmer: standing down + tasks #19-#22)

### No QA tasks on BLACKBOARD

No QA-assigned tasks. Tasks #2-5 pending (Writer/Producer). Tasks #13-14 pending (Writer). Tasks #20-22 pending (Programmer).

### Active inspection (Rule 6)

**CRITICAL FINDING — Narration script deleted unilaterally by Programmer:**

Commit 3554233 (Programmer "standing down") deleted 722 lines from BLACKBOARD.md — the entire narration script (28 scenes, 218 segments). Replaced with single line: "REMOVED (had factual errors, pending rewrite)."

Issues:
1. No QA agent flagged factual errors in the narration. QA's T176 active inspection rated it "excellent quality."
2. If factual errors existed, the correct process would be: create a task for Writer describing the errors, not unilateral deletion.
3. `parse_narration.py` now returns 0 scenes — tested and confirmed. The entire pipeline (parse → TTS → render → assemble) is broken.
4. Task #1 is still marked "done" but its deliverable no longer exists.
5. Tasks #20-#22 (renderer overhaul) depend on scene JSONs, which depend on narration. These cannot proceed without narration.

**New tasks #20-#22 review (not executed, just read descriptions):**
- Task #20: Full render_scene.py rewrite — reasonable scope for a single task
- Task #21: UI overlays (timeline, lower-thirds, watermark) — depends on Task #20
- Task #22: Performance optimization — depends on Task #20
- Communication Log preserved correctly across all 3 commits ✅

### Findings reported to BLACKBOARD (Rule 4.5)

- Task #23: Narration deletion broke pipeline — Writer must rewrite with sources

---

## T180 — Active Inspection (07:30)

**Time**: 2026-06-20 07:30 +08:00
**Type**: Cron trigger (Rule 6 — no pending QA tasks)
**Commits reviewed**: e5847d8 (Task #20: full render_scene.py rewrite)

### No QA tasks on BLACKBOARD

No QA-assigned tasks. Tasks #2-5 pending (Writer/Producer). Tasks #13-14, #23 pending (Writer). Tasks #21-22 pending (Programmer).

### Active inspection (Rule 6)

**Commit e5847d8 — Task #20: Full render_scene.py rewrite (493 lines, +322/-167)**

Complete code review of the new renderer:

**Structure (14 functions):**
- `_load_font`, `get_fonts` — font loading with cache
- `hex_to_rgb`, `lerp_color` — color utilities
- `make_gradient_image` — vertical gradient (per-pixel via `img.load()`)
- `precompute_particles`, `draw_particles_on_image` — particle system
- `build_bg_pixel_row` — pre-computed gradient per row for particle blending
- `wrap_text`, `prewrap_all_segments` — text wrapping
- `ease_in_out` — cosine easing
- `render_frame` — main frame renderer
- `title_alpha_if_first` — title card alpha helper
- `render_scene` — orchestration + ffmpeg pipe
- `main` — CLI entry point

**AST analysis: 14/14 functions called, no dead code ✅**

**Alpha compositing pattern (verified correct):**
- Title card overlay: separate RGBA Image → draw on it → `Image.alpha_composite(img, overlay)` ✅
- Text panel overlay: separate RGBA Image → draw on it → `Image.alpha_composite(img, overlay)` ✅
- Progress bar overlay: separate RGBA Image → draw on it → `Image.alpha_composite(img, overlay)` ✅
- Final `convert("RGB")` only at return (line 366) ✅

**Segment timeline logic:**
- `seg_starts` computed from cumulative `duration_s`
- `time_scale = audio_duration / total_seg_duration` handles audio/segment mismatch
- Segment lookup: iterates `seg_starts`, finds first segment where `t_sec < scaled_end`
- Tested with 3-segment scenario at both `time_scale=1.0` and `time_scale=1.25`
- `seg_progress > 1.0` when audio longer than segments — benign (all downstream uses clamped/guarded) ✅

**Cross-dissolve transitions:**
- Last `SEGMENT_FADE_FRAC` (2%) of segment N: `cross_alpha` ramps 1→0
- First `SEGMENT_FADE_FRAC` of segment N+1: `cross_alpha` ramps 0→1
- Last segment: only fade-out guard (`seg_idx < num_segments - 1`) ✅

**Title card:**
- Shows only during first segment (`seg_idx == 0`)
- Era label, centered title (serif/accent), accent line, subtitle
- ZDF-style fade-in → hold → fade-out using `ease_in_out`
- Text panel appears when `title_alpha > 0.3` (overlap with title) ✅

**Text rendering:**
- Centered word-wrapped text on dark rounded-rectangle panel
- Scroll support for >7 lines with per-line fade at scroll boundaries
- `prewrap_all_segments` caches wrapped lines (performance) ✅

**Minor style notes (not bugs, not reported):**
- `gradient_rgb` parameter in `render_frame()` is unused (dead parameter)
- `import time` inside `render_scene()` body (line 433) instead of module top
- These are cosmetic and don't affect functionality

**Communication Log:** Preserved ✅

### Findings reported to BLACKBOARD

None — no new issues found. Minor style notes logged in journal only.